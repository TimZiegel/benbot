# Copy this file and rename it "env.yaml"

env_variables:

  # Prefix for all commands
  COMMAND_PREFIX:

  # Gcloud project ID
  PROJECT_ID:

  # Path to your Gcloud credentials JSON file
  GOOGLE_CREDENTIALS:

  # Server ID for staging environment
  TEST_SERVER:

  # Token generated by discord (benbot)
  DISCORD_TOKEN:

  # Token generated by discord (testbot)
  TEST_DISCORD_TOKEN: